http.gallery() {
  local parent=~/Downloads/auto
  success_or_log _gallery_impl $1 || return
  get_last_modified $parent/*/
}

_gallery_impl() {
  gallery-dl --cbz \
    -d $parent \
    -f "{num:>04}_{filename}.{extension}" \
    "https://$1" >&2
}